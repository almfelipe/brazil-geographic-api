on: push
  name: Run SSH command
  uses: garygrossgarten/github-action-ssh@v0.6.3
  with:
    # Command to execute on the remote server.
    command: ls
    host: priscillabagano.com.br
    username: prisci34
    port: 22
    privateKey: ${{ secrets.PRIVATE_KEY }}
